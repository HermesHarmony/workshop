name: Forms
icon: page
wysiwyg: true
preview: fields

fields:
  select:
    label: Formular
    type: pages
    multiple: false
    required: true
    query: site.index.filterBy('template', 'in', ['form'])
    create: false
    help: Formulare können unter Dashboard > Formulare erstellt werden.
    